This is da doctor: <%= @doctor.name %>
<br>
These are his appointments:
<ul>
  <% @doctor.appointments.each do |app| %>
  <%= link_to app.appointment_datetime.strftime("%B %d,  %Y at %H:%M") + ' ' + app.patient.name, patient_path(app.patient) %><br>
  <% end %>
</ul>
