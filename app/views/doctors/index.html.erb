<h1>All Doctors</h1>

<u>
<% @doctors.each do |doctor| %>
  <li>
    <%= link_to doctor.name, doctor_path(doctor) %>
    <%= pluralize(doctor.appointments_count, 'appointment')%><br/>
  </li>
<% end %>
</u>
